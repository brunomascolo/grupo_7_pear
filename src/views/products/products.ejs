 <!DOCTYPE html>
<html lang="en">

<%- include("../partials/head.ejs", {titulo: "Pear, sitio de compra y venda de NFT" , cssSheet: "allproducts" }) %>

    <body>
        <%- include("../partials/header.ejs") %>
            <main>
                <!-- Nombre, Precio, Descripcion, archivo (enlace) -->
                <p class="titulo">Listado de NFT publicados</p>
                <div class="container-principal">
                    <!-- Mostrar los productos VISITADOS dinámicamente (usando un for o foreach) -->
                    <% for (let i=0; i < nftFiltrados.length; i++) { %>
                        <div class="productosDetail">
                            <a href="/products/<%= nftFiltrados[i].id %>"><img src="<%= nftFiltrados[i].img %>" alt="slider"
                                    class="imagenDetail"></a>
                            <ul class="info-product">
                                <li>
                                    <a href="/products/<%= nftFiltrados[i].id %>">
                                        <p class="name-nft">
                                            <%= nftFiltrados[i].name %>
                                        </p>
                                    </a>
                                </li>
                                <li>
                                    <p>Creador: <%= nftFiltrados[i].author %>
                                    </p> <!-- Podriamos crear la vista de autores -->
                                </li>
                                <li>
                                    <p>Valor: <i class="fab fa-ethereum"></i> <%= nftFiltrados[i].priceeth %>
                                    </p> <!--  Agregar valor en ETH -->
                                </li>
                                <li class="li-boton">
                                    <a href="/products/edit/<%= nftFiltrados[i].id%>" class="boton-modificar">Modificar</a>
                                </li>
                            </ul>
                        </div>
                        <% } %>
                </div>
            </main>
            <%- include("../partials/footer.ejs") %>
    </body>

</html>
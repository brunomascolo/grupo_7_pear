<!DOCTYPE html>
<html lang="en">

    <%- include("../partials/head.ejs", {titulo: "Pear, sitio de compra y venda de NFT", cssSheet: "create"}) %>

<body>
    <%- include("../partials/header.ejs") %>
    <main>
        <!-- Nombre, Precio, Descripcion, archivo (enlace) -->
        <div class="container-principal">
            <p>Edita tu NFT</p>
            <form action="/products/edit/<%= productToEdit.id %>?_method=PATCH" method="POST" enctype="multipart/form-data" class="formulario">
                <div class="fieldForm">
                    <label class="etiquetas">Nombre de tu producto(*)</label>
                    <input type="text" name="nombrenft" class="textField" value="<%= productToEdit.name %>" required>
                </div>
                <div class="fieldForm">
                    <label class="etiquetas">Categoria(*)</label>
                    <select class="textField" name="categoria">
                        <option value="">Selecciona una catergoria</option>
                        <option value="Art">Arte</option>
                        <option value="Cinema">Cine</option>
                        <option value="Sports">Deportes</option>
                        <option value="Photography">Fotografia</option>
                        <option value="Music">Musica</option>
                        <option value="Videogames">Videojuegos</option>
                    </select>
                </div>
                <div class="fieldForm">
                    <label class="etiquetas">Descripcion(*)</label>
                    <input type="text" class="textField"
                        placeholder="Ingresa una breve descripción del NFT que venderas" name="descripcion" value="<%= productToEdit.description %>">
                </div>
                <div class="fieldForm">
                    <label class="etiquetas">URL <a href="https://ipfs.io/" target="_blank">IPFS</a></label>
                    <input type="text" name="url" class="textField"
                        placeholder="Sugerimos subir tu nft a IPFS y copies tu enlace acá" value="<%= productToEdit.url %>"> 
                </div>
                <!-- <div class="fieldForm">
                    <label class="etiquetas">o subé tu imagen</label>
                    <input type="file" name="imgnft"class="textField">
                </div> -->
                <div class="fieldForm">
                    <label class="etiquetas">Precio(*) ETH</label>
                    <input type="text" name="precioeth" class="textField"
                        placeholder="Tu valor en ETH, lo puedes cambiar cuando quieras" value="<%= productToEdit.priceEth %>">
                </div>
                <div class="botonCrear">
                    <button type="submit" class="botones">Publicar</button>
                </div>
            </form>
        </div>
    </main>
    <%- include("../partials/footer.ejs") %>
</body>

</html>